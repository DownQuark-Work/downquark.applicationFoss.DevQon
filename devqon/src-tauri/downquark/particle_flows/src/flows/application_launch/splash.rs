/*
1. show splash screen
1. config and paths should be stored in state by this point
1. use them to check for base ~/.dq dir and .etc
1. create and /or read template file
1. mock(for now) version check
1. mock(for now) user is logged in -- or maybe this can be read as an opt from the conf?
1. dismiss splash page
...
*/

pub fn init_screen() {

}