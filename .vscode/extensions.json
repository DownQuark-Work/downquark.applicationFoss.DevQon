{
  "recommendations": [
    "tauri-apps.tauri-vscode",
    "rust-lang.rust-analyzer" // to get around rust-analyzer only looking at the root of your project.
    // vscode > shift+cmd+p > user settings JSON > upsert: `rust-analyzer.linkedProjects:["FULL/PATH/TO/CARGO.TOML"]`
  ]
}
