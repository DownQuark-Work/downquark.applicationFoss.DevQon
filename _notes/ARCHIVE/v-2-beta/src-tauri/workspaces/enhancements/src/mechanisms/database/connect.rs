// gets stored config from configure (or dies)
// uses stored config creds to connect to db (or dies)
// returns connection for use with qry

pub fn connect_to_db() {
  println!("[maria ]> Connect to maria database");
  "tester".to_string()
}